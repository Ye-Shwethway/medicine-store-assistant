# Custom GPT Integration

Status: **reserved; not configured in F1**

This directory will hold the future private Medicine Store Assistant Custom GPT Action contract.

Planned contents after the read-only Inventory API is stable:

- `openapi.yaml`
- authentication/security notes
- Action setup instructions
- read-only verification cases

Do not expose arbitrary SQL, database credentials, or production secrets here.

The first Custom GPT experiment must remain read-only and is not part of F1.
