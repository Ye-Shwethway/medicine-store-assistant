# Google Sheets Integration

Status: **reserved; not implemented in F1**

The live Google workbook remains authoritative under the current `$msa` skill until the database is explicitly promoted after shadow validation.

This directory will later contain the controlled Sheet mirror/adapter implementation. It must translate approved Sheet-originated changes into typed backend operations rather than unrestricted last-write-wins synchronization.

No live workbook identifiers, credentials, exports, or private operational data belong in this public repository.
