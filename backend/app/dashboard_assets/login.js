(()=>{const root=document.querySelector('#loginRoot');const form=document.querySelector('#loginForm');const password=document.querySelector('#password');const error=document.querySelector('#loginError');const submit=document.querySelector('#submitButton');const theme=document.querySelector('#themeToggle');if(!root||!form)return;theme?.addEventListener('click',()=>{root.dataset.theme=root.dataset.theme==='dark'?'light':'dark'});async function api(path,opts={}){const response=await fetch(path,{credentials:'same-origin',headers:{'Content-Type':'application/json',...(opts.headers||{})},...opts});let data=null;try{data=await response.json()}catch{}if(!response.ok){const err=new Error((data&&data.detail)||('Request failed: '+response.status));err.status=response.status;throw err}return data}async function bootstrap(){try{const state=await api('/dashboard/api/session');if(state.authenticated){location.replace('/dashboard');return}if(!state.configured){error.textContent='Dashboard access is not provisioned on the server yet.';submit.disabled=true}}catch(e){error.textContent='Unable to read dashboard authentication state.'}}form.addEventListener('submit',async e=>{e.preventDefault();error.textContent='';submit.disabled=true;submit.textContent='Signing in…';try{await api('/dashboard/api/session',{method:'POST',body:JSON.stringify({password:password.value})});password.value='';location.replace('/dashboard')}catch(err){error.textContent=err.status===401?'Sign-in failed. Check your credential and try again.':err.message;submit.disabled=false;submit.textContent='Sign in'}});bootstrap()})();
