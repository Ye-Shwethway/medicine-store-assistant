F7.2 dashboard query note: avoid PostgreSQL nullable bind predicates of the form (:value IS NULL OR column = :value) in dashboard BFF queries. Build optional predicates only when values are present, and use direct equality for the resolved latest migration batch id. This keeps read behavior deterministic and avoids ambiguous/untyped bind failures while preserving read-only/non-canonical semantics.
